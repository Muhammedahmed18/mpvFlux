package app.marlboroadvance.mpvex.ui.theme

import androidx.compose.material3.Typography

/**
 * Typography that uses system font throughout the entire app.
 * This provides consistent integration with the user's system font preferences.
 */
val AppTypography: Typography = Typography()

/**
 * Typography for code/technical displays that also uses system font.
 * Maintains consistency with the rest of the app while using system font everywhere.
 */
val CodeTypography: Typography = Typography()
